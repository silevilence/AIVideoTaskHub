# 持久化 ComfyUI 模板文档与任务快照

ComfyUI 工作流模板以 SQLite 记录管理，其中用户可移植的正文统一保存为“YAML front matter + API 格式 JSON”组合文档；创建任务时再把完整模板文档、变量值、主输出规则和实际连接地址保存为任务快照。相比扫描数据目录文件或让任务始终引用最新模板，这一选择沿用项目现有的数据库备份与 CRUD 边界，同时保留可导入导出的原始文档，并保证模板编辑、停用或删除后，历史任务仍可被准确解释、重试和套用；代价是任务记录会占用更多空间，且模板 schema 演进必须兼容既有快照。
